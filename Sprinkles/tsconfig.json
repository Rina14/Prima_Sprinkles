{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outFile": "Sprinkles.js", /*        Concatenate and emit output to single file. */
  },

  "include": [
    "../FudgeCore/",
    "../FudgeAid/",
    "./*",
]
}